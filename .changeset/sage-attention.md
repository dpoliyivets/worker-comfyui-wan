---
"worker-comfyui": minor
---

Install `sageattention` so the WanVideoWrapper `sageattn` attention mode works at runtime. Required for the cost-optimized Wan2.2 I2V base workflow (FP8 weights + Lightning LoRA + sage-attention + TeaCache).
